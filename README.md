The user can look at the main only.
The input files must be of the form of .coor and .topol as the examples in the repository.
All simulation parameters (tolerance and restart of the gmres, time step, velocity field, forcing term) are defined in the main.
